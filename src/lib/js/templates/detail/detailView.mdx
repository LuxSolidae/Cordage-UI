---
name: Detail View
route: /templates/DetailView
menu: Templates
---


import { Playground, Props } from 'docz';
import { Router } from 'react-router';
import { createBrowserHistory } from "history";



import { ThemeContext } from '@cordage_ui';
import DetailView from './detailView';
import theme from '@cordage_ui/cordage.json';
import logo from '@_src_/lib/img/logo.svg';
import locations from '@_src_/lib/img/icons/locations.svg';
import '@_src_/lib/Docz.css';

# Detail View

<Props of={DetailView} />

<Playground>
  <div className='metro'>
    <ThemeContext.Provider theme={theme}>
      <DetailView
      logo={logo}
      title='Name of group'
      descripton='Groups in Knowledge are groupings of Items and define the schema or structure of the custom data stored in those Items. '
      >
      </DetailView>
    </ThemeContext.Provider>
  </div>
</Playground>

